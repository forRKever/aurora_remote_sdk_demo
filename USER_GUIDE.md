# Aurora Dashboard 使用指南

## 介面概觀

Aurora Dashboard 分為左右兩個面板：

```
┌─────────────────────────────────────────────────────────┐
│                    Aurora Dashboard                      │
├──────────────────┬──────────────────────────────────────┤
│                  │                                        │
│   左側控制面板    │         右側地圖顯示區域              │
│  (280px寬)       │    (即時 2D 地圖 + 操作按鈕)         │
│                  │                                        │
│  ▼ Connection    │  ┌────────────────────────────────┐  │
│  ▼ Pose          │  │                                │  │
│  ▼ Device        │  │   地圖顯示區域                  │  │
│  ▼ Mapping Ctrl  │  │  (占用柵格地圖 + 軌跡)          │  │
│  (新增)          │  │                                │  │
│                  │  └────────────────────────────────┘  │
│                  │                                        │
│                  │  ▼ Map Operations                     │
│                  │    按鈕和進度條                       │
│                  │                                        │
└──────────────────┴──────────────────────────────────────┘
```

---

## 左側面板詳細說明

### 1️⃣ Connection（連接設定）

```
┌─ Connection ────────────────┐
│                              │
│ IP:   [192.168.11.1]        │  ← 输入设备 IP
│ Port: [7447]                 │  ← 输入连接埠
│                              │
│ [Connect]  [Disconnect]      │  ← 連線/斷線
│                              │
│ ● Connected to 192.168.11.1  │  ← 連線狀態
│ (綠色=已連線，紅色=未連線)    │
└──────────────────────────────┘
```

**操作步驟：**
1. 輸入 Aurora 設備的 IP 位址（預設 192.168.11.1）
2. 如需改變連接埠，修改 Port 值（預設 7447）
3. 點擊 **[Connect]** 按鈕
4. 狀態指示燈轉綠色 = 連線成功

**斷線：**
- 點擊 **[Disconnect]** 按鈕

---

### 2️⃣ Pose（位姿資訊）

```
┌─ Pose ──────────────────────┐
│                              │
│ X: 1.234 m                   │  位置 (公尺)
│ Y: 5.678 m                   │
│ Z: 0.123 m                   │
│                              │
│ Roll:  0.12°                 │  旋轉角 (度數)
│ Pitch: -1.23°                │
│ Yaw:   45.67°                │
│                              │
└──────────────────────────────┘
```

**說明：**
- **X, Y, Z**：設備在 3D 空間的座標位置
- **Roll, Pitch, Yaw**：設備的方向角度
- 每 100ms 更新一次
- **實時顯示**，無需操作

---

### 3️⃣ Device Status（設備狀態）

```
┌─ Device Status ──────────────┐
│                              │
│ ● TRACKING                   │  綠色=正常追蹤
│ (紅色=TRACKING LOST)         │  紅色=追蹤遺失
│                              │
│ Firmware: v2.1.1-rtm         │  韌體版本
│ Serial: SN-xxxxxxxxxx        │  序號
│ Uptime: 2h 15m 30s           │  運行時間
│ Map ID: 0                    │  當前地圖編號
│ Keyframes: 245               │  關鍵幀數量
│ Map Points: 12458            │  地圖點數量
│                              │
└──────────────────────────────┘
```

**說明：**
- **SLAM 狀態**：綠色=正常，紅色=丟失
- **Keyframes/Map Points**：地圖建置的質量指標
  - 數字越多表示地圖越詳細
- **實時更新**，無需操作

---

### 4️⃣ Mapping Control（🆕 建圖控制）

```
┌─ Mapping Control ────────────┐
│                              │
│ [Start Mapping] [Stop Map]   │  建圖/定位模式
│                              │
│ [Reset Map]                  │  清除地圖
│                              │
│ Status: Idle                 │  模式狀態
│                              │
└──────────────────────────────┘
```

**功能說明：**

#### 🟢 Start Mapping（開始建圖）
```
按鈕狀態：[Start] 可點擊
↓ 點擊
按鈕狀態：[Start] 灰化，[Stop] 變可點擊
狀態顯示：Status: Mapping mode activated
↓ 效果
設備進入建圖模式，同時建置新地圖
```

**用途：**
- 在新環境中掃描並建造完整地圖
- 邊移動設備邊建圖

#### 🔴 Stop Mapping（停止建圖，進入定位模式）
```
按鈕狀態：[Stop] 可點擊
↓ 點擊
按鈕狀態：[Start] 變可點擊，[Stop] 灰化
狀態顯示：Status: Localization mode activated
↓ 效果
設備進入純定位模式，只在既有地圖上定位
```

**用途：**
- 完成建圖後切換為定位模式
- 設備只進行定位，不再修改地圖

#### ⚪ Reset Map（重置地圖）
```
狀態：隨時可點擊
↓ 點擊
狀態顯示：Status: Map reset
↓ 效果
清除所有現有地圖資料，重新開始
```

**用途：**
- 清除舊地圖，準備建造新地圖
- 出現 SLAM 失敗時的應急操作

---

## 右側地圖區域

### 📍 地圖顯示（主要區域）

```
┌──────────────────────────────────────┐
│                                        │
│     占用柵格地圖 (LiDAR 掃描結果)      │
│   ┌────────────────────────────────┐  │
│   │ ███░░░░░░░░░░░░░░░░░░░░░░░░░  │  │
│   │ ████░░░░░░░░░░░░░░░░░░░░░░░░░  │  │
│   │ █████░░░░░░┌──────┐░░░░░░░░░░  │  │
│   │ █████░░░░░░│ ◆   │░░░░░░░░░░  │  │  ◆ = 當前位置
│   │ █████░░░░░░└──►──┘░░░░░░░░░░  │  │  ► = 方向箭頭
│   │ █████░░░░░░░░░░░░░░░░░░░░░░░░  │  │  ─ = 軌跡路徑
│   │ █████░░░░░░░░░░░░░░░░░░░░░░░░  │  │  ███ = 障礙物
│   │                                  │  │  ░░░ = 空曠區域
│   │ KF: 245 | MP: 12458 | Scale: x  │  │  灰色 = 未探索
│   │ (交叉線 = 中心點)               │  │
│   └────────────────────────────────┘  │
│                                        │
└──────────────────────────────────────┘
```

**地圖顏色說明：**
- **██ 黑色**：障礙物（牆壁、傢俱等）
- **░░ 白色**：空曠區域（可通行）
- **灰色**：未探索區域
- **紅色 ─**：SLAM 追蹤軌跡（關鍵幀位置）
- **綠色 ·**：地圖點（視覺特徵點）
- **🟡 金色 ◆**：當前位置和方向

**控制方式：**

| 操作 | 效果 |
|------|------|
| **滾輪向上** | 放大地圖 (最大 200x) |
| **滾輪向下** | 縮小地圖 (最小 10x) |
| **左鍵拖曳** | 平移地圖視角 |
| **十字線** | 表示地圖中心點 |

**資訊顯示：**
```
KF: 245           ← Keyframes（關鍵幀數）
MP: 12458         ← Map Points（地圖點數）
Scale: 1/80 m     ← 縮放比例（1 像素代表多少公尺）
```

---

### 🎮 Map Operations（地圖操作）

```
┌─ Map Operations ────────────────┐
│                                  │
│ [Refresh Map]                    │
│ ↓                                │
│ [Download Map]                   │
│ 下載地圖到電腦 (.stcm 格式)     │
│                                  │
│ Upload: [Select File..] [Browse] │
│ 上傳地圖到設備                  │
│                                  │
│ [████████░░░░░] 45%             │  進度條（傳輸中）
│                                  │
│ Download complete               │  狀態訊息
│                                  │
└──────────────────────────────────┘
```

#### 🔄 Refresh Map（重新整理地圖）
- **功能**：立即取得最新地圖資料
- **操作**：點擊 **[Refresh Map]**
- **用途**：
  - 地圖顯示延遲時手動更新
  - 驗證最新掃描結果

#### ⬇️ Download Map（下載地圖）
```
操作步驟：
1. 點擊 [Download Map]
2. 選擇儲存位置 + 檔名
3. 等待進度條完成
4. 狀態顯示 "Download complete"
```

**說明：**
- 下載 Aurora 設備上的地圖到電腦
- 格式：`.stcm`（SLAM 地圖格式）
- 可用於：
  - 備份地圖
  - 在其他設備上載入
  - 地圖分析

#### ⬆️ Upload Map（上傳地圖）
```
操作步驟：
1. 點擊 [Browse...]
2. 選擇本機的 .stcm 地圖檔案
3. 檔案名稱顯示在文字框中
4. 等待進度條完成
5. 狀態顯示 "Upload complete"
```

**說明：**
- 從電腦上傳地圖到 Aurora 設備
- 支援格式：`.stcm`
- 用途：
  - 恢復之前備份的地圖
  - 在多個設備間共享地圖

---

## 🔄 典型工作流程

### 情景 1：首次在新環境建圖

```
1. 連線設定
   ├─ 輸入 IP：192.168.11.1
   └─ 點擊 [Connect] → 顯示綠色 ●

2. 開始建圖
   ├─ 點擊 [Start Mapping]
   ├─ 狀態：Status: Mapping mode activated
   └─ 設備進入建圖模式

3. 移動設備進行掃描
   ├─ 在環境中緩慢移動設備
   ├─ 觀察地圖逐漸填充
   ├─ Keyframes 和 Map Points 數字不斷增加
   └─ 圖表顯示掃描過的區域

4. 掃描完成
   ├─ 設備回到起點
   ├─ 地圖不再變化
   └─ 確認掃描完整

5. 停止建圖
   ├─ 點擊 [Stop Mapping]
   ├─ 狀態：Status: Localization mode activated
   └─ 進入定位模式

6. 備份地圖
   ├─ 點擊 [Download Map]
   ├─ 選擇儲存位置
   └─ 等待下載完成
```

### 情景 2：恢復之前的地圖

```
1. 連線設定
   └─ 點擊 [Connect]

2. 上傳地圖
   ├─ 點擊 [Browse...]
   ├─ 選擇 backup_map.stcm
   ├─ 等待上傳完成
   └─ 狀態：Status: Upload complete

3. 定位工作
   ├─ 點擊 [Start Mapping]（進入建圖模式）
   │   或直接在既有地圖上定位
   └─ 設備應用上傳的地圖進行定位
```

---

## ⚠️ 常見問題

### Q1：地圖顯示全黑，沒有任何內容
**原因：** 
- 設備剛連線，地圖資料還未載入
- SLAM 追蹤遺失

**解決：**
```
1. 等待 10 秒，讓地圖資料傳輸
2. 點擊 [Refresh Map]
3. 如果仍然黑色，檢查 Device Status 是否顯示紅色 ● TRACKING LOST
   → 是：點擊 [Reset Map]，重新開始
```

### Q2：SLAM 顯示紅色「TRACKING LOST」
**原因：**
- 環境特徵不足
- 設備移動過快
- 光線不足

**解決：**
```
1. 點擊 [Reset Map]
2. 點擊 [Start Mapping]
3. 緩慢移動設備（速度 < 0.5 m/s）
4. 提供充足光線
```

### Q3：地圖一直在更新，不停變化
**原因：**
- 仍在建圖模式
- 設備還在移動

**解決：**
- 點擊 [Stop Mapping] 進入定位模式

### Q4：無法連線到設備
**檢查清單：**
```
□ IP 位址正確？
□ 設備開機了？
□ 網路連線正常？
□ 連接埠 7447 未被封鎖？
□ 試試點擊 [Disconnect] 再 [Connect]
```

---

## 💡 使用秘訣

1. **最佳建圖速度**：0.3～0.5 m/s（步行速度）
2. **光線要求**：充足自然光或室內照明
3. **環境要求**：避免完全空白或重複的牆面
4. **定期備份**：每次成功建圖後下載地圖
5. **旋轉設備**：建圖時適度旋轉取得完整特徵
6. **清理障礙**：移除地面雜物避免碰撞

---

## 📊 監控重點

| 指標 | 理想值 | 警告值 |
|------|--------|---------|
| Keyframes | > 100 | < 50 |
| Map Points | > 1000 | < 500 |
| FPS | > 10 Hz | < 5 Hz |
| 軌跡連接性 | 連續 | 斷裂 |

---

**祝您使用愉快！** 🎉
