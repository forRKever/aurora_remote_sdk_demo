# Aurora Dashboard 使用指南

## 介面概觀

Aurora Dashboard 分為**左側控制面板**與**右側視覺化區域**，右側分為四個顯示窗格：

```
┌───────────────────────────────────────────────────────────────────────┐
│                         Aurora Dashboard                               │
├───────────────┬───────────────────────────┬───────────────────────────┤
│               │                           │                           │
│  左側控制面板  │   深度相機影像            │  3D VSLAM 地圖            │
│  (280px)      │   (Depth Camera)          │  (MapWidget)              │
│               │                           │                           │
│ ▸ Connection  │                           │  點雲 + 軌跡 + 位置箭頭   │
│ ▸ Pose        │                           │                           │
│ ▸ Device      ├───────────────────────────┤                           │
│ ▸ Mapping     │                           ├───────────────────────────┤
│ ▸ 3D Rec.     │  語意分割影像             │  立體相機預覽             │
│               │  (Semantic Segmentation)  │  (Camera Preview)         │
│               │                           │                           │
│               │  標籤疊加 + 物件中文名    │  左右鏡頭並排顯示         │
│               │                           ├───────────────────────────┤
│               │                           │  Map Operations           │
│               │                           │  (地圖操作按鈕)           │
└───────────────┴───────────────────────────┴───────────────────────────┘
```

各窗格均支援**拖曳分隔線**調整大小，可根據工作需求縮放任意區塊。

---

## 左側面板

### 1. Connection（連線設定）

```
┌─ Connection ──────────────────┐
│ IP:   [192.168.11.1     ]     │  ← Aurora 設備的 IP 位址
│ Port: [7447          ↕ ]     │  ← 連線埠（預設 7447）
│                               │
│ [  Connect  ]  [Disconnect]   │
│                               │
│ ● Connected to 192.168.11.1   │  ← 連線狀態指示
└───────────────────────────────┘
```

| 欄位 | 說明 |
|------|------|
| IP | Aurora 設備的 IP 位址，出廠預設 `192.168.11.1` |
| Port | TCP 連線埠，預設 `7447`，一般不需修改 |
| 狀態指示 | 綠色 `●` = 已連線；紅色 `●` = 未連線或斷線 |

**操作步驟：**
1. 確認主機與設備在同一網段（直連或同 Wi-Fi）
2. 輸入設備 IP，按 **[Connect]** 建立連線
3. 狀態文字轉為綠色 `● Connected` 即成功
4. 斷線時按 **[Disconnect]**，或直接關閉程式（程式會自動清理連線）

---

### 2. Pose（位姿資訊）

```
┌─ Pose ────────────────────────┐
│ X:     1.234 m                │  ← 世界座標 X 軸位置
│ Y:     5.678 m                │  ← 世界座標 Y 軸位置
│ Z:     0.890 m                │  ← 世界座標 Z 軸（高度）
│                               │
│ Roll:   0.12°                 │  ← 橫滾角（繞 X 軸旋轉）
│ Pitch: -1.23°                 │  ← 俯仰角（繞 Y 軸旋轉）
│ Yaw:   45.67°                 │  ← 偏航角（繞 Z 軸旋轉，即水平朝向）
│                               │
│ Quality: GOOD (±0.042m)       │  ← 位置精度指標（見下方說明）
└───────────────────────────────┘
```

**座標系說明（Aurora 世界座標，右手座標系）：**

| 軸向 | 說明 |
|------|------|
| **Z** | 垂直向上（高度），始終固定 |
| **X** | 水平面內，設備初始朝向方向（前方） |
| **Y** | 水平面內，X 軸右轉 90° 方向（右方），由右手定則決定：X × Y = Z（向上）|

| 旋轉角 | 說明 |
|--------|------|
| **Roll** | 橫滾角，繞 X 軸旋轉（左傾/右傾） |
| **Pitch** | 俯仰角，繞 Y 軸旋轉（抬頭/低頭） |
| **Yaw** | 偏航角，繞 Z 軸旋轉（逆時針為正，即向左轉） |

> **在俯視地圖（按 T 鍵）中的顯示：**
> - Aurora X 軸 → 螢幕水平方向（向右）
> - Aurora Y 軸 → 螢幕垂直方向（向上，北方）
> - 此顯示方式與 Aurora SDK 官方 `demo/map_render` 的 2D 地圖一致

單位：公尺（m）；角度：度（°）或弧度（rad）

**Quality 精度指標（95% 信心半徑）：**

| 顯示 | 顏色 | 意義 | 95% 誤差範圍 |
|------|------|------|-------------|
| EXCELLENT | 綠色 | 極佳 | < 5 cm |
| GOOD | 黃綠 | 良好 | 5～10 cm |
| FAIR | 橘色 | 普通 | 10～30 cm |
| POOR | 紅色 | 差 | > 30 cm |

> **注意**：POOR 通常表示環境特徵不足或剛完成重定位，建議緩慢移動幾秒讓 SLAM 穩定。

更新頻率：**每 100ms** 一次（10 Hz）。

---

### 3. Device Status（設備狀態）

```
┌─ Device Status ───────────────┐
│ SLAM: ● TRACKING              │  ← 追蹤狀態（綠=正常，紅=遺失）
│                               │
│ Firmware: v2.1.1-rtm          │  ← 韌體版本
│ Serial:   SN-XXXXXXXXXX       │  ← 設備序號
│ Uptime:   2h 15m 30s          │  ← 設備連續運行時間
│ Map ID:   0                   │  ← 目前使用的地圖編號
│ Keyframes: 245                │  ← 全域關鍵幀數（愈多愈好）
│ Map Points: 12458             │  ← 全域地圖點數（愈多愈好）
│ Floor 1/2 | H:0.05m | 87%    │  ← 樓層偵測（見下方說明）
└───────────────────────────────┘
```

**SLAM 追蹤狀態：**
- `● TRACKING`（綠）：VSLAM 正常運作，位置可信
- `● TRACKING LOST`（紅）：追蹤中斷，位置不可靠，建議緩慢移動或執行 Reset Map

**Floor 樓層偵測：**
```
Floor 1/2 | H:0.05m | 87%
  │    │      │         │
  │    │      │         └── 偵測信心度（百分比）
  │    │      └──────────── 當前樓層高度（m）
  │    └─────────────────── 總樓層數
  └──────────────────────── 目前所在樓層編號
```
- 系統依 VSLAM 軌跡的高度分布，自動識別樓層
- 多樓層建圖時可用來確認設備位於哪一層

**Keyframes / Map Points 品質參考：**

| 指標 | 初期 | 良好 | 優秀 |
|------|------|------|------|
| Keyframes | < 50 | 100～500 | > 500 |
| Map Points | < 500 | 5,000～20,000 | > 20,000 |

---

### 4. Mapping Control（建圖控制）

```
┌─ Mapping Control ─────────────┐
│ [Start Mapping] [Stop Mapping]│  ← 建圖模式 / 定位模式切換
│                               │
│ [      Reset Map       ]      │  ← 清除所有地圖資料
│                               │
│ [   Clear Depth Cloud  ]      │  ← 清除累積的深度點雲
│                               │
│ Status: Idle                  │  ← 目前模式狀態
└───────────────────────────────┘
```

#### [Start Mapping]（開始建圖）
- 設備進入**建圖模式**：同時追蹤位置並擴展地圖
- 適合：在新環境初次掃描，或重返環境後繼續補充地圖
- 按下後按鈕灰化，[Stop Mapping] 變為可點擊
- 狀態顯示：`Status: Mapping mode activated`

#### [Stop Mapping]（停止建圖，進入定位模式）
- 設備進入**純定位模式**：地圖凍結，只在現有地圖上計算位置
- 適合：建圖完成後的日常使用，避免不必要的地圖更新
- 狀態顯示：`Status: Localization mode activated`

#### [Reset Map]（重置地圖）
- 清除設備上的**所有地圖資料**，從零開始
- 無法復原，建議操作前先 **Download Map** 備份
- 適用情境：
  - SLAM 追蹤嚴重失敗，需要重新建圖
  - 更換場域，需要全新地圖
- 狀態顯示：`Status: Map reset`

#### [Clear Depth Cloud]（清除深度點雲）
- 清除 MapWidget 中**累積的橘色深度點雲**（不影響 VSLAM 地圖）
- 深度點雲由深度相機即時累積，會隨時間不斷增加
- 適用情境：
  - 移動到新區域，舊點雲位置不再有參考價值
  - 點雲數量達到 30 萬上限，手動清空後重新累積
  - 調整掃描範圍前先歸零

---

### 5. 3D Recording - COLMAP（3D 錄製）

```
┌─ 3D Recording (COLMAP) ───────┐
│ [Start Recording] [Stop]      │
│                               │
│ Status: Recording... | KF:45  │  ← 錄製中狀態（含已錄關鍵幀數）
└───────────────────────────────┘
```

COLMAP 錄製功能會將**相機影像 + 位姿資訊**以 COLMAP 格式儲存到本機資料夾，供離線進行高精度 3D 重建（SfM / MVS）。

**操作步驟：**
1. 點擊 **[Start Recording]**，選擇輸出資料夾
2. 狀態顯示 `Recording...` 並計數關鍵幀數
3. 掃描完成後點擊 **[Stop]**
4. 使用 [COLMAP](https://colmap.github.io/) 或 [OpenMVS](https://github.com/cdcseacave/openMVS) 等工具進行離線重建

> **注意**：錄製期間會產生大量圖片檔案，請確保儲存裝置有足夠空間（每分鐘約 50～200 MB）。

---

## 右側視覺化區域

### 深度相機影像（Depth Camera）— 左上

顯示深度相機的即時深度圖，以 **JET 色彩映射**（藍→綠→黃→紅）表示距離遠近：

```
顏色對應距離：
藍色  → 近距離（靠近設備）
綠色  → 中距離
黃色  → 較遠
紅色  → 最遠（或超出量測範圍）
黑色  → 無效深度（遮擋、反光過強等）
```

- 更新頻率：約 **5 Hz**（每 200ms）
- 解析度：依設備型號而定（通常 640×480）
- 用途：確認深度感知是否正常、觀察近距障礙物、驗證點雲來源

---

### 語意分割影像（Semantic Segmentation）— 左下

顯示 AI 即時場景分割結果，每個語意類別以**固定顏色**標示，並疊加文字說明：

```
┌──────────────────────────────────┐
│  [圖像畫面]                      │
│                                  │
│  主要物件: Chair 椅子             │  ← 畫面中占比最大的類別
│                                  │
│  floor 地板 / wall 牆壁 /        │  ← 偵測到的物件標籤（英文+中文）
│  ceiling 天花板 / chair 椅子 ... │
└──────────────────────────────────┘
```

- 更新頻率：約 **5 Hz**（每 200ms）
- 支援類別：依設備韌體版本而異（通常 15～80 種常見室內物件）
- 標籤語言：英文原名 + 對應繁體中文翻譯同步顯示
- 用途：了解環境語意組成、輔助地圖標注、場域分析

---

### 3D VSLAM 地圖（MapWidget）— 右上

這是最主要的地圖視窗，以 **3D OpenGL** 渲染 VSLAM 稀疏點雲、深度累積點雲與設備軌跡：

#### 視覺元素說明

```
┌─────────────────────────────────────────────────────────────┐
│ KF: 245 | MP: 12458 | DC: 87432 | Az: 45 | El: 30 | Dist: 8.5m │  ← 統計資訊列
│                                                               │
│   藍→紅漸層小點                   ← VSLAM 稀疏地圖點 (MP)    │
│   橘色小點（更密集）              ← 深度相機累積點雲 (DC)     │
│   橘色線條                        ← 設備移動軌跡               │
│   紅色實心點（較大）              ← 關鍵幀位置 (KF)            │
│   金黃色實心大點 + 箭頭           ← 當前設備位置與朝向         │
│   深灰藍色方格線                  ← 參考地面網格（1m 間距）    │
│                                                               │
│   左下角 XYZ 軸：X=紅 Y=綠 Z=藍  ← 座標方向參考指示器        │
└─────────────────────────────────────────────────────────────┘
```

#### 統計資訊列（頂部顯示）詳細說明

```
KF: 245 | MP: 12458 | DC: 87432 | Az: 45 | El: 30 | Dist: 8.5m
 │           │           │          │         │         │
 │           │           │          │         │         └── 鏡頭到中心點距離（公尺）
 │           │           │          │         └──────────── 鏡頭仰角（-90°～90°，正=俯看）
 │           │           │          └────────────────────── 鏡頭水平旋轉角（0°～360°）
 │           │           └───────────────────────────────── 累積深度點雲點數
 │           └───────────────────────────────────────────── VSLAM 稀疏地圖點數
 └───────────────────────────────────────────────────────── VSLAM 關鍵幀數
```

**各參數詳細說明：**

| 參數 | 全名 | 說明 | 理想值 |
|------|------|------|--------|
| **KF** | Keyframes | VSLAM 關鍵幀數，表示設備停留並產生地圖的位置數量 | > 100 |
| **MP** | Map Points | VSLAM 稀疏特徵點數，構成地圖骨架，數量愈多地圖愈精確 | > 5,000 |
| **DC** | Depth Cloud | 由深度相機累積的 3D 點數（橘色點），反映場域結構密度 | 10,000～300,000 |
| **Az** | Azimuth | 3D 視圖鏡頭的水平旋轉角度（度），0°=正北方向 | 操作參考 |
| **El** | Elevation | 3D 視圖鏡頭的俯仰角度（度），0°=水平，90°=正上方俯視 | 操作參考 |
| **Dist** | Distance | 3D 視圖鏡頭到觀察中心點的距離（公尺），越小越放大 | 操作參考 |

#### 點雲顏色說明

| 顏色 | 來源 | 大小 | 說明 |
|------|------|------|------|
| 藍→紅漸層 | VSLAM 稀疏地圖（MP） | 2.5px | 藍=低處，紅=高處（依高度著色） |
| 橘色漸層 | 深度相機累積點雲（DC） | 1.5px | 深度相機偵測到的表面點，密集表示牆壁/物件 |
| 橘紅線條 | 移動軌跡 | 線段 | 設備移動路徑（關鍵幀連線） |
| 紅色實心點 | 關鍵幀位置（KF） | 6px | 每個關鍵幀的精確位置 |
| 金黃色大點 | 當前位置 | 12px | 設備目前位置 |
| 金黃色箭頭 | 當前朝向 | 線段 | 指向設備目前面對的方向（Yaw） |

#### 3D 視圖操作方式

| 操作 | 效果 |
|------|------|
| **左鍵拖曳** | 旋轉視角（改變 Az / El） |
| **右鍵拖曳** | 平移觀察中心點 |
| **滑鼠滾輪** | 縮放（Dist 範圍：0.5m ～ 100m） |
| **T 鍵** | 切換至正上方俯視（El=90°），觀察平面地圖輪廓 |
| **F 鍵** | 切換至正面視角（El=0°） |
| **R 鍵** | 重置視角（Az=45°, El=30°, Dist=10m） |
| **H 鍵** | 顯示/隱藏快捷鍵說明 |

> **建圖技巧**：建圖完成後按 **T** 切換俯視，深度點雲（橘色）可呈現出房間平面輪廓，類似 2D 地圖效果。

#### 深度點雲說明

深度點雲是本系統無 LiDAR 情況下，替代 2D 地圖的場域建構機制：

```
運作原理：
  深度相機 → 每幀取每 8×8 像素的 1 個 3D 點
  →  過濾無效距離（< 0.3m 或 > 6m）
  →  依當前 VSLAM Pose 轉換至世界座標
  →  累積至全域緩衝（上限 30 萬點）
  →  在 MapWidget 以橘色顯示
```

- 更新頻率：**5 Hz**（每 200ms 新增一批）
- 達到 30 萬點上限後，自動移除最舊的 20%（6 萬點）後繼續累積
- 按 **[Clear Depth Cloud]** 可手動清空，從頭重新累積

---

### 立體相機預覽（Camera Preview）— 右中

顯示設備搭載的**左右立體相機**即時影像，以左右並排方式顯示：

```
┌────────────────────────────────┐
│  [左鏡頭影像]  [右鏡頭影像]   │
│                                │
│   (灰階或彩色，視硬體型號)      │
└────────────────────────────────┘
```

- 用途：確認設備視野、觀察環境是否有足夠視覺特徵（紋理、對比）
- 若畫面過暗或特徵稀少（白牆、反光面），SLAM 容易追蹤失敗

---

### Map Operations（地圖操作）— 右下

```
┌─ Map Operations ──────────────────────────┐
│ [Refresh Map]                              │
│ [Download Map]                             │
│ Upload: [______.stcm______] [Browse...]    │
│ [Export Map as PNG]                        │
│ [████████████░░░░░░] 60%                  │  ← 傳輸進度（進行中才顯示）
│ Download complete                          │  ← 操作結果訊息
└────────────────────────────────────────────┘
```

#### [Refresh Map]（重新整理地圖）
- 立即向設備請求最新的 VSLAM 關鍵幀與地圖點資料
- 正常情況下每 10 秒自動更新一次，手動觸發可即時看到最新結果

#### [Download Map]（下載地圖到電腦）
```
操作步驟：
1. 點擊 [Download Map]
2. 在對話框選擇儲存位置並輸入檔名
3. 等待進度條完成（視地圖大小，通常 5～60 秒）
4. 狀態顯示 "Download complete"
```
- 下載格式：`.stcm`（SLAMTEC 私有地圖格式）
- 建議：每次建圖完成後立即備份

#### [Browse... / Upload]（上傳地圖到設備）
```
操作步驟：
1. 點擊 [Browse...] 選擇本機 .stcm 檔案
2. 檔名顯示在文字框中後，操作自動開始上傳
3. 等待進度條完成
4. 狀態顯示 "Upload complete"
```
- 上傳完成後設備會自動載入上傳的地圖
- 可用於：還原備份、在多台設備間共享地圖

#### [Export Map as PNG]（匯出地圖圖片）
- 將目前 MapWidget 的**3D 視圖截圖**儲存為 PNG 圖片
- 截圖前建議先按 **T** 切換至俯視，可獲得類似平面地圖的效果

---

## 典型工作流程

### 情景 1：首次建立場域地圖

```
1. 連線
   └─ 輸入 IP → [Connect] → 等待綠色 ●

2. 重置舊地圖（若有殘留）
   └─ [Reset Map] → Status: Map reset

3. 開始建圖
   └─ [Start Mapping] → Status: Mapping mode activated

4. 移動設備掃描場域
   ├─ 緩慢移動（建議 0.3～0.5 m/s）
   ├─ 觀察 KF、MP、DC 數字持續增加
   ├─ VSLAM 地圖（藍→紅點）逐漸擴展
   └─ 深度點雲（橘色）填充場域結構

5. 完成建圖
   ├─ [Stop Mapping] → 進入定位模式
   └─ 按 T 鍵切換俯視，確認地圖完整

6. 備份地圖
   └─ [Download Map] → 選擇儲存位置
```

### 情景 2：小範圍手持掃描（測試/驗證）

```
1. 連線 → [Start Mapping]

2. 手持設備緩慢左右搖晃、前後傾斜
   ├─ 確保 Quality: GOOD 或以上
   ├─ 觀察 DC 點數快速增加
   └─ 橘色點雲在正面牆壁/物件上逐漸成形

3. 按 T 俯視確認點雲形狀
   └─ 可看到前方環境的局部平面輪廓

注意：手持小範圍掃描僅覆蓋視野內局部區域，
       地圖完整性約 20～30%，適合功能驗證用途。
```

### 情景 3：恢復既有地圖並定位

```
1. 連線

2. 上傳地圖
   ├─ [Browse...] 選擇 .stcm 檔案
   └─ 等待 "Upload complete"

3. 進入建圖/定位模式
   ├─ [Start Mapping]（若需要繼續擴充地圖）
   └─ [Stop Mapping]（若只需要在既有地圖上定位）

4. 等待 SLAM 重新定位
   └─ Quality 指標從 POOR 逐漸回升至 GOOD 或以上
```

---

## 常見問題

### Q1：連線後地圖完全空白，沒有任何點
**可能原因：**
- 地圖資料還未傳輸完成（連線後約需 5～15 秒）
- 設備上沒有建立過地圖

**解決：**
1. 等待 15 秒後點擊 **[Refresh Map]**
2. 若仍空白，確認 Device Status 中 Keyframes > 0
3. 若 Keyframes = 0，代表設備沒有地圖 → 點擊 **[Start Mapping]** 開始建圖

---

### Q2：SLAM 顯示 TRACKING LOST（紅色）
**可能原因：**
- 移動速度過快（> 1 m/s）
- 環境特徵太少（白牆、暗處、反光面）
- 視野遮擋或方向轉變過急

**解決：**
1. 保持靜止 2～3 秒，等待自動恢復
2. 若未恢復，緩慢轉動到有豐富特徵的方向
3. 仍無法恢復 → **[Reset Map]** → **[Start Mapping]** 重新建圖

---

### Q3：深度點雲（DC）數字沒有增加
**可能原因：**
- 設備深度相機不支援 POINT3D 格式
- 目標物距離不在有效範圍（0.3m ～ 6m）

**確認方法：**
1. 觀察左上深度相機視窗是否有正常影像（非全黑）
2. 確保前方有物體在 0.5～5m 範圍內
3. 若深度圖正常但 DC 持續為 0，設備可能不支援此功能

---

### Q4：無法連線到設備
**檢查清單：**
```
□ Aurora 設備已開機並完成啟動（等待約 30 秒）
□ 電腦與設備在同一網段（192.168.11.x）
□ Ping 192.168.11.1 有回應
□ 防火牆未封鎖 Port 7447
□ 試試 [Disconnect] 後再 [Connect]
□ 重啟設備後重試
```

---

### Q5：Quality 一直顯示 POOR
**可能原因：**
- 剛完成重定位，SLAM 尚未收斂
- 環境特徵不足，無法精確估計協方差

**解決：**
1. 在有豐富紋理的環境中緩慢移動 10～20 秒
2. 確認 MP 數量在增加（表示地圖仍在擴展）
3. 若 MP 不再增加且 Quality 仍差，嘗試 **[Reset Map]** 重新建圖

---

## 監控指標速查

| 指標 | 位置 | 初始 | 良好 | 優秀 |
|------|------|------|------|------|
| SLAM 狀態 | Device Status | TRACKING LOST | TRACKING | TRACKING |
| Quality | Pose | POOR | FAIR | EXCELLENT/GOOD |
| Keyframes（KF） | Device Status / 地圖右上 | 0～20 | 50～200 | > 200 |
| Map Points（MP） | Device Status / 地圖右上 | 0～500 | 3,000～15,000 | > 15,000 |
| Depth Cloud（DC） | 地圖右上 | 0 | 10,000～100,000 | > 100,000 |
| Dist（視圖距離） | 地圖右上 | 任意 | 5～15m | 依場域大小調整 |

---

## 使用秘訣

1. **建圖速度**：越慢越好，0.3 m/s 為佳，保持 SLAM 有足夠時間處理每幀
2. **光線充足**：室內照明均勻，避免逆光或強烈陰影
3. **豐富紋理**：在特徵豐富的牆面（海報、書架、傢俱）附近啟動建圖
4. **閉環掃描**：移動路徑形成閉環（回到起點），SLAM 會進行迴環校正，大幅提升地圖精度
5. **俯視確認**：建圖途中隨時按 **T** 切換俯視，確認深度點雲是否如預期覆蓋場域
6. **定期備份**：每次成功建圖後立即 **[Download Map]** 儲存，避免設備重啟後遺失
7. **深度雲清除**：移動到新區域後，考慮按 **[Clear Depth Cloud]** 清空舊點雲，讓新區域點雲更清晰
